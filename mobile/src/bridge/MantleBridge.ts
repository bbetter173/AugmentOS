/**
 * Stub module so Jest can resolve `@/bridge/MantleBridge` (see jest.setup.js).
 * Runtime app wiring may use a different entry; do not import this from production code unless intended.
 */
const MantleBridge = {}
export default MantleBridge
